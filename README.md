# Crud_ops_MongoDB

Crud Operation using the mongo DB

1. Create the mongo DB and using the username and password get the url.
2. Create the collection in the Db server.

Make the .env file and set MONGODB_URL & PORT
